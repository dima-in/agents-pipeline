# 2026-05-28

- Confirmed multi-developer routing fix in run `run_20260528_092116`: TASK-001 now routes only `infra-developer` and `test-developer`; `code-developer` is no longer invoked for reference-only `database.py`/`models.py`. Developer deterministic checks passed before QA.
- Fixed QA false-negative after `run_20260528_092116`: contract `must_contain` checks now validate Alembic/SQLAlchemy snippets semantically instead of raw substrings, and QA/template-validator get larger direct_api `read_files` excerpts with truncation markers. Added regression tests; full suite passed with `venv\Scripts\python.exe -m pytest`.
- Fixed infra-developer retry ambiguity after `run_20260528_095431`: infra prompt now requires module-level Alembic `revision` and `down_revision` assignments, and migration validation feedback explicitly says docstring `Revision ID`/`Revises` is not enough. Added regression coverage; full suite passed with `venv\Scripts\python.exe -m pytest`.
- Implemented canonical implementation backlog persistence for agents-pipeline: validated planner output now saves `.agents-pipeline/projects/<project_id>/state/implementation_backlog.json`, new runs reuse it unless `--fresh-run`/`regenerate_backlog`, completed tasks are recorded in `completed_implementation_tasks.json`, and diagnostics report canonical/completed/selected task state. Requested suite passed: `venv\Scripts\python.exe -m pytest tests\test_workflow.py tests\test_agent_reports.py tests\test_multi_developer_pipeline.py`.
- Tightened implementation task memory end-to-end: successful implementation now fails if the completed registry cannot be written, completion can recover the selected task from canonical backlog `selected_task_id`, explicit completed reruns require `--rerun-completed`, and `--mark-selected-complete` exists for manual recovery. Requested suite passed with `251 passed`.
- Fixed canonical backlog selection edge case: stored `implementation_backlog.json:selected_task_id` is now diagnostic/recovery state, not an implicit CLI task id. If it points to a completed task, the run skips it and selects the first incomplete task; only explicit `--task-id` requires `--rerun-completed` for completed tasks. Requested suite passed with `252 passed`.
- Added regression coverage that explicit `--task-id TASK-002` reaches the final multi-developer plan as `task=TASK-002`, while preserving canonical backlog completed-task skipping semantics. Requested suite passed with `253 passed`.
- Fixed direct_api retrieval noise where `list_files` could expose nested vendor directories such as `frontend/node_modules` even though repo_map excluded them. `list_files` and `search_text` now share repo_map exclusion logic; suite passed with `262 passed`.
- Fixed the remaining architect prompt noise source: repository-context tree generation itself now excludes vendor/runtime paths, so `frontend/node_modules` is absent before retrieval starts. Verified context generation and suite passed with `262 passed`.
