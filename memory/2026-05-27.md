# 2026-05-27

- Fixed implementation multi-developer prompt/context issue in agents-pipeline: code/infra/test developer agents now receive selected-task contract context and scoped file excerpts instead of relying on noisy prior agent context.
- Fixed multi-developer routing to treat selected `allowed_paths` as editable paths, so existing code files such as `gateway-v4/app/models.py` are routed to `code-developer` instead of being skipped when they are part of the task contract.
- Added regression coverage in `tests/test_multi_developer_pipeline.py`; full suite passed with `venv\Scripts\python.exe -m pytest tests`.
- Follow-up fix on `feature/json-multi-developer-pipeline`: scoped multi-developer overrides now filter `reason_each_path_is_needed`, keep target_file as read-only context for `test-developer`, clear out-of-scope target/test contract fields for sibling agents, and reject `code-developer` no-op when it owns app paths but has no scoped target contract. Full suite passed with `venv\Scripts\python.exe -m pytest`.
- Fixed strict-mode retrieval block from run `run_20260527_172747`: `code-developer` no longer auto-enters strict mode when it owns two application files, and multi-developer prompts now tell agents to use one `read_files` request instead of repeated `read_file` calls. Full suite passed with `venv\Scripts\python.exe -m pytest`.
- Fixed brittle migration test generation from run `run_20260527_173519`: test-developer static validation now rejects format-sensitive raw string assertions for Alembic/SQLAlchemy calls and the default rollback dirty strategy is `stash` so failed attempts with generated changes do not stop retries. Full suite passed with `venv\Scripts\python.exe -m pytest`.
- Fixed repeated multi-developer failures from run `run_20260527_175242`: changed scoped files are now deterministically validated immediately after each sub-agent, so code syntax errors, invalid migrations, and brittle tests stop at the responsible agent instead of surfacing later as mixed synthetic developer failures. Full suite passed with `venv\Scripts\python.exe -m pytest`.
- Fixed multi-developer routing to treat `allowed_paths` as the write boundary, not as actionable work. Reference-only paths such as `gateway-v4/app/database.py` and `gateway-v4/app/models.py` no longer route `code-developer`; code agents run only when `target_file`/new files actually point to code. Full suite passed with `venv\Scripts\python.exe -m pytest`.
