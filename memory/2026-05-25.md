# 2026-05-25

- Work continued on `feature/json-multi-developer-pipeline` for the agents-pipeline observability and multi-developer experiment.
- User needs console output that shows prompt, feedback, logs, and agent actions clearly in Russian, with noisy raw details moved to report files.
- Important bug found: `code-developer`/`infra-developer`/`test-developer` direct API writes were not treated as completion, causing false `Exceeded retrieval rounds` after successful `write_file`/`apply_patch`.
- Fix direction: multi-developer agents now use direct edit tools; successful scoped write should produce `status=implemented`, and feedback previews are shown inline in boxed operator output.
- Added strict execution mode for narrow implementation agents to prevent costly retrieval loops. In strict mode `list_files`/`search_text` are blocked, only one `read_file`/`read_files` operation is allowed, retrieval budget diagnostics are persisted, and `strict_retrieval_blocked` stops retry attempts instead of burning another model call.
- Full project verification with `venv\Scripts\python.exe -m pytest` passed: 237 tests.
- Fixed deterministic tool extraction for mixed model outputs. The orchestrator now recovers `write_file` and `apply_patch` from fenced JSON, prose+JSON, escaped JSON strings, and mixed multi-operation replies, and preserves write operations by returning `tool_batch` when a response mixes reads with a write.
