# Copy this file to `.env` (already gitignored) and fill in the values.
# start.py loads `.env` from the engine root and the launch directory at startup.
# Real environment variables always WIN over `.env` — a stale file never shadows them.
OPENROUTER_API_KEY=
